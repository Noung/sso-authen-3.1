# ===================================================================
#  Dependencies (ไฟล์ที่ถูกติดตั้งโดย Package Managers)
# ===================================================================

# โฟลเดอร์สำหรับ PHP dependencies ที่ติดตั้งโดย Composer
# (ไม่ใส่ / ข้างหน้า เพื่อให้ข้ามทุกโฟลเดอร์ vendor ที่อยู่ในโปรเจกต์)
vendor/

# โฟลเดอร์สำหรับ Node.js dependencies ที่ติดตั้งโดย NPM
# (ไม่ใส่ / ข้างหน้า เพื่อให้ข้ามทุกโฟลเดอร์ node_modules)
node_modules/


# ===================================================================
#  Environment Files (ไฟล์ที่เก็บข้อมูลความลับ ไม่ควรเก็บใน Git)
# ===================================================================

# ไฟล์ Environment ที่มักใช้เก็บ API Keys, Database Passwords
.env
.env.local
.env.*.local


# ===================================================================
#  Log Files (ไฟล์บันทึกการทำงานของโปรแกรม)
# ===================================================================

*.log
logs/


# ===================================================================
#  IDE/Editor and OS Files (ไฟล์ตั้งค่าของโปรแกรมและระบบปฏิบัติการ)
# ===================================================================

# VS Code
.vscode/

# macOS
.DS_Store

# Windows
Thumbs.db